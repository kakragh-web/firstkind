// program to find the LCM of two integers

// take input
const numl = prompt('Enter a first positive integer: ');
const num2 = prompt( 'Enter a second positive integer: ');

// higher number among numberi and number2 is stored in min
let min = (num1 › num2) ? num : numz;

// while loop
while (true) l
if (min % num1 == 0 && min % num2 == 0) {
console. log(*The LCM of $(num} and $(num2} is $(min)*);
     break;
}
min++;
}
